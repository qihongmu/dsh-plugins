# @deepseek-ai/dsh-client-remotes-scheduled-task: plugins-remotes-scheduled-task — merged into the profile patch list by `dsh plugin` once
# this package is added to dsh.profile.bundles.
- insert:
    - id: plugins-remotes-scheduled-task
      name: '@deepseek-ai/dsh-client-remotes-scheduled-task'
