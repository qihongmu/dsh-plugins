/**
 * Node-half stub: the UI package exists for its browser bundle; the loader
 * entry imports this module harmlessly on the host side.
 * @module @qihongmu/dsh-client-ui-token-tracing
 */

export function apply(): void {}
