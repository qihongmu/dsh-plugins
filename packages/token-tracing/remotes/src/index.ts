/**
 * Node-half stub: the remotes package exists for its browser bundle; the
 * loader entry imports this module harmlessly on the host side.
 * @module @qihongmu/dsh-client-remotes-token-tracing
 */

export function apply(): void {}
